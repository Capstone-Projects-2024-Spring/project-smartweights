name: CI

on:
  push:
    branches:
      - testing

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Clean
      uses: xcodebuild clean -scheme SmartWeights -workspace project.xcworkspace
